title: Interrupt controller with one cell
version: 1

description:

properties:
    compatible:
        constraint: "interrupt,one-cell"
        type: string-array
        generation: define

"#cells":
  - one
