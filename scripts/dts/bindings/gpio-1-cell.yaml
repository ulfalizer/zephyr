title: GPIO controller with one cell
version: 1

description:

properties:
    compatible:
        constraint: "gpio,one-cell"
        type: string-array
        generation: define

"#cells":
  - one
