title: Interrupt controller with three cells
version: 1

description:

properties:
    compatible:
        constraint: "controller,three-cell"
        type: string-array
        generation: define

"#cells":
  - one
  - two
  - three
