title: GPIO controller with two cells
version: 1

description:

properties:
    compatible:
        constraint: "gpio,two-cell"
        type: string-array
        generation: define

"#cells":
  - one
  - two
