title: Clock source with two cells
version: 1

description: Clock source with two cells

properties:
    compatible:
        constraint: "clock-two-cell"
        type: string-array

"#cells":
  - one
  - two
